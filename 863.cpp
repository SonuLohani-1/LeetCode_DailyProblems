#include <bits/stdc++.h>
#include "myClasses.h"
using namespace std;
class Solution
{
public:
    vector<int> distanceK(TreeNode *root, TreeNode *target, int k)
    {
        // if the target is at level i then 
    }
};
int main()
{
    vector<int> arr = {3, 5, 1, 6, 2, 0, 8, NULL, NULL, 7, 4};
    TreeNode *root = buildTree(arr);
    return 0;
}